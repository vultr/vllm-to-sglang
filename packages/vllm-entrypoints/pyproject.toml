[project]
name = "vllm-entrypoints"
version = "0.0.1"
requires-python = ">=3.12"
dependencies = ["vllm-shim"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/vllm"]
